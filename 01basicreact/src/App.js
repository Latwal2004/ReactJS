import Chai from './chai'
function App() {
  return (
    <>
    <Chai />
    <h1>My first react code | Harish Latwal</h1>
    <p>since we can only return single element same fragments concept is used here if you want multiple tags to return wrap it into a fragment. </p>
    <p>Same Naming convention of function with capital letter is recomended otherwise it will not be rendered</p>
  </>
  );
}

export default App;
